import AdminLogin from '@/components/admin/admin-login';

export const metadata = {
  title: 'Admin Login | Secret Network',
  description: 'Panel de administración de Secret Network',
};

export default function AdminLoginPage() {
  return <AdminLogin />;
}
